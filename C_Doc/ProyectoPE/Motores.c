//
// Created by pollofeliz on 20/04/25.
//

#include "Motores.h"
#include "UsuarioDTO.h"
/**
const Paramsmotor motores_registrados[TOTAL_MOTORES] = {
    {
        .nombre = "1.8L i-VTEC",
        .fabricante = "Honda",
        .cilindrada = 1.8f,
        .compresionOriginal = 190.0f,
        .tipoCombustible = 0,             // 0 = Gasolina
        .tipoPieza = CULATA,              // Definido en enum TipoPieza
        .material = "Aluminio",
        .desgaste = 0.09f,                // 9% de desgaste
        .tolerancia = 0.10f,
        .medidaOriginal = 81.0f,
        .medidaActual = 80.93f,
        .necesitaRectificacion = 0       // No necesita rectificación
    },{
        .nombre = "3.2L Duratorq",
        .fabricante = "Ford",
        .cilindrada = 3.2f,
        .compresionOriginal = 410.0f,
        .tipoCombustible = 1,             // 1 = Diesel
        .tipoPieza = MONOBLOCK,           // Definido en enum TipoPieza
        .material = "Hierro fundido",
        .desgaste = 0.14f,                // 14% de desgaste
        .tolerancia = 0.12f,
        .medidaOriginal = 89.9f,
        .medidaActual = 89.76f,
        .necesitaRectificacion = 1       // Sí necesita rectificación
    }
    /**
{
    //Campos vacios

    Motor piezaUsuario2 = inicializarMotor(1, 2, "Aluminio", 0.1000f, 0.1050f, 0.1250f, 0.1150f, 1, 0);
    //piezaUsuario2.tipoPieza = CULATA;
    Culata* pzc2 = inicializarCulata(piezaUsuario2, 18, 0.12, 1);

    },
{
    //Campos vacios
    /**
    .id_Usuario = ,
    .nombre = ,
    .fabricante = ,
    .cilindrada = ,
    .compresionOriginal = ,
    .numeroSerie = ,
    .tipoCombustible = ,
    .tipoPieza = ,
    .material = ,
    .desgaste = ,
    .tolerancia = ,
    .medidaOriginal = ,
    .medidaActual = ,
    .necesitaRectificacion =    

    }
    };
    */

//Motor piezaUsuario2 = inicializarMotor(1, 2, "Aluminio", 0.1000f, 0.1050f, 0.1250f, 0.1150f, 1, 0);
//piezaUsuario2.tipoPieza = CULATA;
//Culata* pzc2 = inicializarCulata(piezaUsuario2, 18, 0.12, 1);


// const Culata culatas_registradas[TOTAL_MOTORES] = {
// {
//         // Culata asignada al motor 0
//         //.motor = inicializarMotor(motores_registrados[0], 2, 3, "UVJX45"),
//         .motor = NULL,
//         .numValvulas = 4,
//         .presionPrueba = .123,
//         .tieneFisuras = 0

//     },
// {
//     //.motor = inicializarMotor(motores_registrados[1], 3, 4 , "KJLO986"),
//     .motor = NULL,
//     .numValvulas = 18,
//     .presionPrueba = 0.12,
//     .tieneFisuras = 1
//     }
// };
